<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trainerguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileScreen"></activity>
        <activity android:name=".TraineesScreen"/>
        <activity android:name=".TrainerScreen"/>

        <activity android:name=".HomeScreen">

        </activity>
        <activity android:name=".TrainerGuideScreen"></activity>
        <activity android:name=".ForgotPasswordForm"></activity>
        <activity android:name=".RegistrationForm"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectProfileType"/>
    </application>

</manifest>